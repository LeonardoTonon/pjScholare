/**
 * @format
 */

import { AppRegistry } from 'react-native';
import Navigation from './src/navigation';
import { name as pjscholare } from './app.json';

AppRegistry.registerComponent(pjscholare, () => Navigation);
